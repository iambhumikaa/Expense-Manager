<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="page for editing your Expenses">
    <title>Edit Income</title>
    <link href="/main.css" rel="stylesheet">
</head>

<body style="display: flex; align-items: center; justify-content: center;">
    <form class="expensesEdit_form" id="expensesForm" action="/income/<%= income._id %>?_method=PATCH" method="POST"
        aria-labelledby="edit-income-form-heading">
        <div class="form_heading_div">
            <h1 class="form_heading">Edit Income</h1>
        </div>
        <div class="form_sub_divs">
            <label for="category">Category</label>
            <input class="inputField" type="text" id="category" name="category" placeholder="Enter category"
                value="<%= income.category %>" required>
        </div>
        <div class="form_sub_divs">
            <label for="amount">Amount</label>
            <input class="inputField" type="number" id="amount" name="amount" placeholder="$ Amount here"
                value="<%= income.amount %>" required>
        </div>
        <div class="form_sub_divs">
            <label for="description">Description</label>
            <textarea class="inputField" id="description" name="description" placeholder="Description..."
                rows="4"><%= income.description %></textarea>
        </div>
        <div class="form_sub_divs">
            <label for="date">Date</label>
            <input class="inputField" type="date" name="date" id="date"
                value="<%= income.date.toISOString().split('T')[0] %>" required>
        </div>
        <div class="income_add_btn_div">
            <button type="submit" class="income_add_btn" style="color: white;">UPDATE</button>
        </div>
    </form>
</body>

</html>